app:
  auth:
    jwt:
      secret: ${JWT_SECRET}
      access-token-expiry: 15m
      refresh-token-expiry: 14d
---
spring:
  config:
    activate:
      on-profile: dev
  security:
    user:
      name: user
      password: test