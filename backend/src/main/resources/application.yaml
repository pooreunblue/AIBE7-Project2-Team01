spring:
  application:
    name: backend
  profiles:
    active: dev,db,auth,redis,storage
---
spring:
  config:
    activate:
      on-profile: dev
    import: optional:file:.env[.properties]
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB